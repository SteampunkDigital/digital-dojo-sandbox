# Flask web server
flask>=3.0.0

# MongoDB
pymongo>=4.6.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client for Ollama API
requests>=2.31.0

# Async support (optional, for background tasks)
celery[redis]>=5.3.0

# Image processing
pillow>=10.0.0

# PLY file handling (for splat merging)
plyfile>=1.0.0

# SAM (Segment Anything Model) for object masking
# pip install git+https://github.com/facebookresearch/segment-anything.git
# Download checkpoint: https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth

# PyTorch (for SD and SAM3D - install separately based on CUDA version)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Diffusers for Stable Diffusion (install when needed)
# pip install diffusers transformers accelerate

# Note: SAM3D dependencies should be installed from the SAM3D repo
